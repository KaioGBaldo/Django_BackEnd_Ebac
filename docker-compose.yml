version: '3.8'

services:
  db:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=bookstore
      - POSTGRES_USER=kaio
      - POSTGRES_PASSWORD=suasenha
    networks:
      - bookstore_network  # Conecta o banco à rede

  web:
    build: .
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - POSTGRES_NAME=bookstore
      - POSTGRES_USER=kaio
      - POSTGRES_PASSWORD=suasenha
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
    depends_on:
      - db
    networks:
      - bookstore_network  # Conecta a aplicação à mesma rede

# --- ESTA É A PARTE QUE O PROFESSOR PEDIU ---
networks:
  bookstore_network:
    driver: bridge

volumes:
  postgres_data: